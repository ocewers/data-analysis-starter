# Environment Variables for Data Analysis Starter Kit
# Copy this file to .env and adjust values as needed
# 
# IMPORTANT: .env files are gitignored to protect sensitive information
# Never commit .env files with actual credentials or sensitive data

# ============================================================================
# Project Directory Paths
# ============================================================================

# Project Root Directory
# By default, automatically detected by finding pyproject.toml
# Uncomment to override:
# PROJECT_ROOT=/path/to/project

# Data Directory
# Default: {PROJECT_ROOT}/data
# Can be absolute or relative to PROJECT_ROOT
# DATA_DIR=data
# DATA_DIR=/absolute/path/to/data

# Output Directory
# Default: {PROJECT_ROOT}/output
# OUTPUT_DIR=output

# Notebooks Directory
# Default: {PROJECT_ROOT}/notebooks
# NOTEBOOKS_DIR=notebooks

# Source Code Directory
# Default: {PROJECT_ROOT}/src
# SRC_DIR=src

# Tests Directory
# Default: {PROJECT_ROOT}/tests
# TESTS_DIR=tests

# ============================================================================
# Team Collaboration Examples
# ============================================================================

# Example: Using a shared data directory on a network drive
# DATA_DIR=/mnt/shared/team-project-data
# OUTPUT_DIR=/mnt/shared/team-project-outputs

# Example: Using cloud storage paths (after mounting)
# DATA_DIR=/mnt/s3/my-bucket/project-data
# OUTPUT_DIR=/mnt/s3/my-bucket/project-outputs

# Example: Windows network paths
# DATA_DIR=Z:/shared/project-data
# OUTPUT_DIR=Z:/shared/project-outputs

# Example: macOS network paths
# DATA_DIR=/Volumes/shared/project-data
# OUTPUT_DIR=/Volumes/shared/project-outputs

# ============================================================================
# Database Connections (Team Projects)
# ============================================================================

# Example: PostgreSQL connection
# DATABASE_URL=postgresql://user:password@localhost:5432/database_name

# Example: MySQL connection
# DATABASE_URL=mysql://user:password@localhost:3306/database_name

# Example: SQLite connection (relative or absolute path)
# DATABASE_URL=sqlite:///./data/local.db

# ============================================================================
# API Keys and Credentials (Team Projects)
# ============================================================================

# Example: API keys for external services
# API_KEY=your_api_key_here
# API_SECRET=your_api_secret_here

# Example: Cloud provider credentials
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_DEFAULT_REGION=us-east-1

# GCP_PROJECT_ID=your_project_id
# GCP_CREDENTIALS_PATH=/path/to/credentials.json

# AZURE_STORAGE_CONNECTION_STRING=your_connection_string

# ============================================================================
# Project-Specific Configuration
# ============================================================================

# Add your project-specific environment variables below:
# 
# Example:
# MODEL_VERSION=v2.1
# FEATURE_SET=baseline
# ENVIRONMENT=development
